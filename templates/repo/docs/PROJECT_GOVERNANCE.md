# Project Governance

This repository uses Playbook v0.1.0 to enforce lightweight governance.

## Verify rule

When files under `src/**`, `app/**`, `server/**`, or `supabase/**` change,
`docs/PLAYBOOK_NOTES.md` must be updated in the same change.
