{
  "name": "playbook-monorepo",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "smoke": "node scripts/smoke-cli.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
